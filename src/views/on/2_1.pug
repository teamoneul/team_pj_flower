extends baseOn
block style 
    include 2_1.css
block content
    .background
        .modal
            span.pink 꽃병을 만들려면
            span.pink2 로그인이 필요해요
            form(method="POST") 
                p.id 아이디
                input#id(type="text" required name="id" placeholder="id")
                p.pw 비밀번호
                input#pw(type="password" required name="password" placeholder="password")
                input.login(type="submit" value="로그인")
            button.nonAcc(onClick="location.href='join'") 계정이 없다면?
