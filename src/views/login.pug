extends base
block content 
    h1  login
        form(method="POST")
            input(type="text" required name="id" placeholder="id")
            input(type="password" required name="password" placeholder="password")
            input(type="submit")
          
    hr
    div     
        span Don't have an account?
        a(href="/join") Create now